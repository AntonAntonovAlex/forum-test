export enum AppRoute {
  Main = '/',
  PostPage = '/post-page/:id',
};

export enum APIRoute {
  Posts = '/posts',
  Comments = '/comments',
  PostCommens = '/posts/',
  Users = '/users',
};

export enum NameSpace {
  Data = 'DATA',
};